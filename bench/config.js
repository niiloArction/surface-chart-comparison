const BENCHMARK_CONFIG = {
  library: "lcjs",
  // "static" | "append" | "refresh"
  mode: "append",
  sampleSize: 5,
  sampleHistory: 50,
  ticksEnabled: false,
  appendNewSamplesPerSecond: 1,
  yAxisInterval: [0, 1.2],
};
